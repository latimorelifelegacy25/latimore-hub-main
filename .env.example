# ─── Database ────────────────────────────────────────────────────────────────
# Supabase pooled connection (port 6543) — used at runtime
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/postgres?pgbouncer=true"
# Supabase direct connection (port 5432) — used for migrations only
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"

# ─── Auth ─────────────────────────────────────────────────────────────────────
NEXTAUTH_SECRET="run: openssl rand -base64 32"
NEXTAUTH_URL="https://yourdomain.com"
GOOGLE_CLIENT_ID="your-google-oauth-client-id"
GOOGLE_CLIENT_SECRET="your-google-oauth-client-secret"
# Comma-separated list of Google emails allowed admin access
ADMIN_EMAILS="you@example.com"

# ─── Webhooks ─────────────────────────────────────────────────────────────────
# HMAC-SHA256 secret for verifying Fillout webhook signatures
FILLOUT_SECRET="your-fillout-webhook-secret"
GOOGLE_CHAT_WEBHOOK_URL=""

# ─── Email (Resend) ───────────────────────────────────────────────────────────
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxx"
# Address that receives internal lead notification emails
LEAD_NOTIFY_EMAIL="jackson1989@latimorelegacy.com"
NOTIFY_TO="you@yourdomain.com"
# From address used for both notification and thank-you emails (must be verified in Resend)
THANKYOU_FROM="noreply@yourdomain.com"

# ─── AI ──────────────────────────────────────────────────────────────────────
# Set to "openai" (default) or "gemini"
AI_PROVIDER="openai"
OPENAI_API_KEY="sk-..."
# Optional: override the default model (gpt-4.1-mini)
OPENAI_MODEL="gpt-4.1-mini"
GEMINI_API_KEY="AIza..."
# Optional: override the default model (gemini-1.5-flash)
GEMINI_MODEL="gemini-1.5-flash"
# Allows cron jobs / scheduled routes to bypass admin session auth
CRON_SECRET="your-cron-secret"

# ─── Client-side tracking ────────────────────────────────────────────────────
NEXT_PUBLIC_BASE_URL="https://yourdomain.com"
# Optional Google Analytics 4 measurement ID
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
NEXT_PUBLIC_GA4_ID="G-XXXXXXXXXX"
# Google Tag Manager container ID (used by @next/third-parties/google)
NEXT_PUBLIC_GTM_ID="GTM-XXXXXXX"

# ─── External analytics API (optional) ───────────────────────────────────────
# Public-safe — used in Client Components and the PublicTracker
# Set to your provider's ingest endpoint (Segment, Mixpanel, custom, etc.)
NEXT_PUBLIC_ANALYTICS_API_URL="https://api.yourdomain.com/v1"
NEXT_PUBLIC_ANALYTICS_KEY="your_public_tracking_key_here"
# Secret — used only in Server Actions and API Routes
SECRET_ANALYTICS_WRITE_KEY="your_secret_backend_key_here"

# ─── Token encryption ────────────────────────────────────────────────────────
# AES-256-GCM key for encrypting OAuth access/refresh tokens at rest in the DB.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If unset, tokens are stored as plain text (backward compatible but not recommended for production).
TOKEN_ENCRYPTION_KEY=""

# ─── Dev overrides ────────────────────────────────────────────────────────────
# Set to "true" to skip admin auth check in local development
DISABLE_ADMIN_AUTH="false"
# ─── Database (Supabase / Prisma) ────────────────────────────────────────────
# Pooled connection (port 6543) — used at runtime
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct connection (port 5432) — used for migrations only
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# ─── Supabase (for PAHS standalone table or direct Supabase calls) ────────────
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."
SUPABASE_SERVICE_ROLE_KEY="eyJ..."   # service_role secret — never expose client-side

# ─── NextAuth / Admin Auth ────────────────────────────────────────────────────
NEXTAUTH_URL="https://hub.latimorelifelegacy.com"
NEXTAUTH_SECRET=""                   # generate: openssl rand -base64 32

# Comma-separated list of Google accounts allowed to access /admin
ADMIN_EMAILS="jackson@latimorelifelegacy.com"

# Set true ONLY for local/debug — never in production
DISABLE_ADMIN_AUTH=false

# ─── Google OAuth (for NextAuth Google provider) ─────────────────────────────
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ─── Google Calendar (optional — for appointment sync) ───────────────────────
GOOGLE_CALENDAR_ID=""
GOOGLE_SERVICE_ACCOUNT_EMAIL=""
GOOGLE_SERVICE_ACCOUNT_KEY=""        # full JSON key as single line

# ─── Email (Resend) ───────────────────────────────────────────────────────────
RESEND_API_KEY="re_..."
LEAD_NOTIFY_EMAIL="jackson1989@latimorelegacy.com"
NOTIFY_TO="jackson@latimorelifelegacy.com"
THANKYOU_FROM="Latimore Life & Legacy <noreply@latimorelifelegacy.com>"

# Legacy aliases (used by pahs-lead route — falls back to NOTIFY_TO/THANKYOU_FROM)
LEAD_NOTIFY_TO=

LEAD_NOTIFY_FROM=

# ─── AI Provider ─────────────────────────────────────────────────────────────
# Use "openai" (default) or "gemini"
AI_PROVIDER=openai
OPENAI_API_KEY="sk-..."
GEMINI_API_KEY=""

# ─── PAHS Lead Routing ────────────────────────────────────────────────────────
# "crm"      → writes Contact/Inquiry/Task/Event into the Hub CRM via Prisma
# "supabase" → writes to a standalone Supabase table (PAHS_LEADS_TABLE)
PAHS_LEAD_TARGET=crm
PAHS_LEADS_TABLE=pahs_leads            # only used when PAHS_LEAD_TARGET=supabase

# ─── Rate Limiting (Upstash Redis) ───────────────────────────────────────────
# If set, rate limiting uses Upstash Redis (persistent, works across instances/edge).
# If unset, falls back to in-memory limiting (single-instance only).
UPSTASH_REDIS_REST_URL=""            # from Upstash console → REST API → URL
UPSTASH_REDIS_REST_TOKEN=""          # from Upstash console → REST API → Token

# ─── Webhooks & Cron ─────────────────────────────────────────────────────────
FILLOUT_SECRET=""                    # HMAC-SHA256 secret from Fillout dashboard
CRON_SECRET=""                       # shared secret for cron job auth header

# ─── Notion Worker (CRM contact sync) ────────────────────────────────────────
# Protects /api/internal/contacts, which the Notion Worker (workers/src/index.ts)
# polls for the contacts backfill + delta sync. Generate: openssl rand -base64 32
# Set the SAME value on the worker side with: ntn workers env set INTERNAL_API_SECRET=...
INTERNAL_API_SECRET=""
# Not used by this app — set on the Notion Worker side (ntn workers env set
# APP_BASE_URL=...) to this app's deployed base URL so it can reach /api/internal/contacts.

# ─── Public / Client-side ────────────────────────────────────────────────────
NEXT_PUBLIC_BASE_URL="https://hub.latimorelifelegacy.com"
NEXT_PUBLIC_GA4_ID=""                # legacy alias — prefer NEXT_PUBLIC_MAIN_GA4_ID

# GA4 — three-property setup per SPEC-2
NEXT_PUBLIC_MAIN_GA4_ID="G-S0Q3E4DEBJ"     # main website (source of truth)
NEXT_PUBLIC_CAMPAIGN_GA4_ID="G-D4SCQZT4T9" # campaign / funnel / landing pages (optional)
NEXT_PUBLIC_APP_GA4_ID="G-KVM9W2SM21"      # admin / internal product analytics (optional)

# ─── Social Engagement / SPEC-2 ──────────────────────────────────────────────
# Engagement sync token (used by external schedulers to POST /api/analytics/social/sync)
ENGAGEMENT_SYNC_TOKEN=""

# Weekly report generation token (for external cron trigger)
WEEKLY_REPORT_TOKEN=""

# Document upload protection (not used server-side; token is for optional API gateway)
DOCUMENT_UPLOAD_TOKEN=""

# ─── Meta / Facebook ─────────────────────────────────────────────────────────
META_GRAPH_VERSION="v25.0"          # Override only if Meta deprecates/changes the default Graph API version

# Facebook App credentials (Meta App Dashboard → Settings → Basic)
FB_APP_ID=""                    # App ID — used for OAuth dialog + token inspection
FB_APP_SECRET=""                # App Secret — used for token exchange + HMAC verification

# OAuth flow: callback URL must be registered in your Meta App's Valid OAuth Redirect URIs
# e.g.  https://hub.latimorelifelegacy.com/api/social/facebook/callback

# Meta webhook verification (Lead Ads)
META_APP_SECRET=""              # Same as FB_APP_SECRET if using a single Meta app
META_PAGE_ACCESS_TOKEN=""       # Fallback page-level access token (long-lived)
META_WEBHOOK_VERIFY_TOKEN=""    # Random string you set in Meta webhook config

# LinkedIn lead gen bridge
LINKEDIN_INGEST_TOKEN=""        # Bearer token for /api/social/linkedin/ingest

# AI model routing (3-tier: Gemini → OpenAI → Claude)
ANTHROPIC_API_KEY=""            # Used as final escalation for sentiment analysis
GEMINI_SENTIMENT_MODEL="gemini-2.0-flash-lite"
OPENAI_SENTIMENT_MODEL="gpt-4.1-nano"
CLAUDE_SENTIMENT_MODEL="claude-haiku-4-5-20251001"
