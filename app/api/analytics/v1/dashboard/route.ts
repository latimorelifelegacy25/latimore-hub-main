export const dynamic = 'force-dynamic'

// Preserve the existing dashboard endpoint while sourcing its data from
// the live first-party operational analytics pipeline.
export { GET } from '../live/route'
